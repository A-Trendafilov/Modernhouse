import ServicePage from "@/components/sections/ServicePage";

const ModularHouse = () => (
  <ServicePage
    translationPrefix="pages.modularHouse"
    features={["factory", "fast", "expandable", "custom", "energy", "transport"]}
  />
);

export default ModularHouse;
