import HeroSlider from "./HeroSlider";

const HeroSection = () => {
  return (
    <div className="relative overflow-hidden w-full h-[70vh] sm:h-[80vh] md:h-screen">
      <HeroSlider />
    </div>
  );
};

export default HeroSection;
